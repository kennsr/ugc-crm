// Default values used across the app — never hardcode these inline.
// Import from '@/lib/const/default'.

export const DEFAULT_VIDEO_PAY_RATE = 30;
export const DEFAULT_VIDEO_STATUS = 'backlog';

export const DEFAULT_USD_IDR_RATE = 17000;
export const DEFAULT_EDITOR_RATE = 0.2;
export const DEFAULT_OPS_COST_RATIO = 0.05;

export const DEFAULT_PLATFORM = 'tiktok';
export const DEFAULT_CURRENCY = 'USD';

export const INVITE_EXPIRY_DAYS = 7;
export const INVITE_EXPIRY_MS = INVITE_EXPIRY_DAYS * 24 * 60 * 60 * 1000;
